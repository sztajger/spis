#include <stdio.h>
#include <string.h>
#include "book_mngr.c"



int main()
{
    // For testig purpose only
   /*
    book_t book1 =
    {
        .author = "Tom",
        .book_name = "Hary Poper",
        .year = 111
    };
    add_book(book1);
    display_books(books, book_cnt);
    delete_last_book(book1);
    display_books(books, book_cnt);
    */
    return 0;
}